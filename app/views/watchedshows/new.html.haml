.row
  = form_for @add_watched_show do |f|
    .small-2
      = f.label :rating
      = f.select :rating, options_for_select(@rating)
    .small-2
      = f.label :tvshow
      = f.text_field :tvshow
    .actions
      = f.submit "Add watched show", class: "button tiny"
